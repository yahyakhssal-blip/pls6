const {SlashCommandBuilder}=require("discord.js");module.exports={data:new SlashCommandBuilder().setName("leaderboard").setDescription("ترتيب الأغنى"),async execute(i,c){let d=c.store?c.store.balances():JSON.parse(fs.readFileSync("./data/balances.json"));await i.reply("🏆 Leaderboard: "+Object.entries(d).sort((a,b)=>b[1]-a[1]).slice(0,10).map((x,n)=>`\n${n+1}. <@${x[0]}> — ${x[1]} درهم`).join(""))}};